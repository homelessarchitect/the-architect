# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_brand(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.brand.serialize import serialize_brand
    return serialize_brand(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_brands(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        is_active: Annotated[bool | None, "Filter by is_active"] = None,
    ) -> list[dict[str, Any]]:
        """List brands with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.brand.repository import BrandRepository
            repo = BrandRepository(session)
            filters: dict[str, Any] = {}
            if is_active is not None:
                filters["is_active"] = is_active
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_brand(r) for r in results]

    @mcp.tool()
    async def get_brand(
        id: Annotated[str, "The UUID of the brand to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single brand by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.brand.repository import BrandRepository
            repo = BrandRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"Brand with id '{id}' not found"}
        return _serialize_brand(result)

    @mcp.tool()
    async def create_brand(
        name: Annotated[str, "name"],
        description: Annotated[str | None, "description"] = None,
        voice_profile: Annotated[dict, "tone, personality_traits[], yes_phrases[], no_phrases[]"] = {},
        content_pillars: Annotated[list, "[{name, description, examples[]}]"] = [],
        visual_identity: Annotated[dict, "color_palette, typography, style_notes"] = {},
        platforms: Annotated[list, "['instagram', 'tiktok', 'linkedin', 'newsletter']"] = [],
        is_active: Annotated[bool, "is_active"] = True,
    ) -> dict[str, Any]:
        """Create a new brand."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.brand.models import Brand
            from factory.generated.aces.brand.repository import BrandRepository
            entity = Brand(
                name=name,
                description=description,
                voice_profile=voice_profile,
                content_pillars=content_pillars,
                visual_identity=visual_identity,
                platforms=platforms,
                is_active=is_active,
            )
            repo = BrandRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_brand(result)

    @mcp.tool()
    async def update_brand(
        id: Annotated[str, "The UUID of the brand to update"],
        name: Annotated[str | None, "Updated name"] = None,
        description: Annotated[str | None, "Updated description"] = None,
        voice_profile: Annotated[dict | None, "Updated voice_profile"] = None,
        content_pillars: Annotated[list | None, "Updated content_pillars"] = None,
        visual_identity: Annotated[dict | None, "Updated visual_identity"] = None,
        platforms: Annotated[list | None, "Updated platforms"] = None,
        is_active: Annotated[bool | None, "Updated is_active"] = None,
    ) -> dict[str, Any]:
        """Update an existing brand."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.brand.repository import BrandRepository
            from uuid import UUID as _UUID
            repo = BrandRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"Brand with id '{id}' not found"}
            if name is not None:
                entity.name = name
            if description is not None:
                entity.description = description
            if voice_profile is not None:
                entity.voice_profile = voice_profile
            if content_pillars is not None:
                entity.content_pillars = content_pillars
            if visual_identity is not None:
                entity.visual_identity = visual_identity
            if platforms is not None:
                entity.platforms = platforms
            if is_active is not None:
                entity.is_active = is_active
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_brand(entity)

    @mcp.tool()
    async def delete_brand(
        id: Annotated[str, "The UUID of the brand to delete"],
    ) -> dict[str, Any]:
        """Delete a brand by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.brand.repository import BrandRepository
            from uuid import UUID as _UUID
            repo = BrandRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"Brand with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
