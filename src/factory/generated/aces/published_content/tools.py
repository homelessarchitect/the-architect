# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from datetime import datetime
from typing import Annotated, Any
from uuid import UUID


def _serialize_published_content(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.published_content.serialize import serialize_published_content
    return serialize_published_content(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_published_contents(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        piece_id: Annotated[str | None, "Filter by piece_id"] = None,
    ) -> list[dict[str, Any]]:
        """List published_contents with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.published_content.repository import PublishedContentRepository
            repo = PublishedContentRepository(session)
            filters: dict[str, Any] = {}
            if piece_id is not None:
                from uuid import UUID as _UUID
                filters["piece_id"] = _UUID(piece_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_published_content(r) for r in results]

    @mcp.tool()
    async def get_published_content(
        id: Annotated[str, "The UUID of the published_content to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single published_content by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.published_content.repository import PublishedContentRepository
            repo = PublishedContentRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"PublishedContent with id '{id}' not found"}
        return _serialize_published_content(result)

    @mcp.tool()
    async def create_published_content(
        piece_id: Annotated[str, "UUID of piece"],
        platform: Annotated[str, "platform"],
        published_at: Annotated[datetime, "published_at"],
        url: Annotated[str | None, "url"] = None,
        platform_post_id: Annotated[str | None, "Native platform ID for fetching metrics"] = None,
    ) -> dict[str, Any]:
        """Create a new published_content."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.published_content.models import PublishedContent
            from factory.generated.aces.published_content.repository import PublishedContentRepository
            from uuid import UUID as _UUID
            entity = PublishedContent(
                piece_id=_UUID(piece_id) if piece_id is not None else None,
                platform=platform,
                published_at=published_at,
                url=url,
                platform_post_id=platform_post_id,
            )
            repo = PublishedContentRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_published_content(result)

    @mcp.tool()
    async def update_published_content(
        id: Annotated[str, "The UUID of the published_content to update"],
        platform: Annotated[str | None, "Updated platform"] = None,
        published_at: Annotated[datetime | None, "Updated published_at"] = None,
        url: Annotated[str | None, "Updated url"] = None,
        platform_post_id: Annotated[str | None, "Updated platform_post_id"] = None,
    ) -> dict[str, Any]:
        """Update an existing published_content."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.published_content.repository import PublishedContentRepository
            from uuid import UUID as _UUID
            repo = PublishedContentRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"PublishedContent with id '{id}' not found"}
            if platform is not None:
                entity.platform = platform
            if published_at is not None:
                entity.published_at = published_at
            if url is not None:
                entity.url = url
            if platform_post_id is not None:
                entity.platform_post_id = platform_post_id
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_published_content(entity)

    @mcp.tool()
    async def delete_published_content(
        id: Annotated[str, "The UUID of the published_content to delete"],
    ) -> dict[str, Any]:
        """Delete a published_content by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.published_content.repository import PublishedContentRepository
            from uuid import UUID as _UUID
            repo = PublishedContentRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"PublishedContent with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
