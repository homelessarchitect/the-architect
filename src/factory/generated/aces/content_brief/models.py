# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

import uuid
from datetime import datetime
from enum import StrEnum

from sqlalchemy import (
    DateTime,
    String,
    Text,
    ForeignKey,
    Index,
    UniqueConstraint,
    func,
)
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from factory.core.database import Base



class BriefObjective(StrEnum):
    AWARENESS = "awareness"
    ENGAGEMENT = "engagement"
    TRAFFIC = "traffic"
    CONVERSION = "conversion"
    EDUCATION = "education"


class StorytellingFramework(StrEnum):
    AIDA = "AIDA"
    PAS = "PAS"
    BAB = "BAB"
    STORYBRAND = "StoryBrand"
    PSP = "PSP"


class ContentBrief(Base):
    __tablename__ = "aces_content_briefs"

    id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    idea_id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_content_ideas.id", ondelete="CASCADE"), nullable=False)
    brand_id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_brands.id", ondelete="CASCADE"), nullable=False)
    objective: Mapped[str] = mapped_column(String(50), nullable=False, default=BriefObjective.AWARENESS.value)
    storytelling_framework: Mapped[str] = mapped_column(String(50), nullable=False, default=StorytellingFramework.PSP.value)
    hook_options: Mapped[list] = mapped_column(JSONB, nullable=False)
    outline: Mapped[list] = mapped_column(JSONB, nullable=False)
    cta: Mapped[str] = mapped_column(Text, nullable=False)
    keywords: Mapped[list] = mapped_column(JSONB, nullable=False)
    references: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
    notes: Mapped[str | None] = mapped_column(Text, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        Index("ix_aces_content_briefs_brand_id", "brand_id"),
        UniqueConstraint("idea_id", name="uq_aces_content_briefs_idea_id"),
    )
