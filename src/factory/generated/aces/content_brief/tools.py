# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_content_brief(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.content_brief.serialize import serialize_content_brief
    return serialize_content_brief(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_content_briefs(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        brand_id: Annotated[str | None, "Filter by brand_id"] = None,
        idea_id: Annotated[str | None, "Filter by idea_id"] = None,
    ) -> list[dict[str, Any]]:
        """List content_briefs with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_brief.repository import ContentBriefRepository
            repo = ContentBriefRepository(session)
            filters: dict[str, Any] = {}
            if brand_id is not None:
                from uuid import UUID as _UUID
                filters["brand_id"] = _UUID(brand_id)
            if idea_id is not None:
                from uuid import UUID as _UUID
                filters["idea_id"] = _UUID(idea_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_content_brief(r) for r in results]

    @mcp.tool()
    async def get_content_brief(
        id: Annotated[str, "The UUID of the content_brief to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single content_brief by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_brief.repository import ContentBriefRepository
            repo = ContentBriefRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"ContentBrief with id '{id}' not found"}
        return _serialize_content_brief(result)

    @mcp.tool()
    async def create_content_brief(
        idea_id: Annotated[str, "UUID of idea"],
        brand_id: Annotated[str, "UUID of brand"],
        cta: Annotated[str, "Low-friction call to action"],
        objective: Annotated[str, "objective"] = "awareness",
        storytelling_framework: Annotated[str, "storytelling_framework"] = "PSP",
        hook_options: Annotated[list, "3-5 hook variations [{type, text}]"] = [],
        outline: Annotated[list, "Ordered beats [{beat, content}]"] = [],
        keywords: Annotated[list, "keywords"] = [],
        references: Annotated[dict | None, "references"] = None,
        notes: Annotated[str | None, "notes"] = None,
    ) -> dict[str, Any]:
        """Create a new content_brief."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_brief.models import ContentBrief
            from factory.generated.aces.content_brief.repository import ContentBriefRepository
            from uuid import UUID as _UUID
            entity = ContentBrief(
                idea_id=_UUID(idea_id) if idea_id is not None else None,
                brand_id=_UUID(brand_id) if brand_id is not None else None,
                objective=objective,
                storytelling_framework=storytelling_framework,
                hook_options=hook_options,
                outline=outline,
                cta=cta,
                keywords=keywords,
                references=references,
                notes=notes,
            )
            repo = ContentBriefRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_content_brief(result)

    @mcp.tool()
    async def update_content_brief(
        id: Annotated[str, "The UUID of the content_brief to update"],
        objective: Annotated[str | None, "Updated objective"] = None,
        storytelling_framework: Annotated[str | None, "Updated storytelling_framework"] = None,
        hook_options: Annotated[list | None, "Updated hook_options"] = None,
        outline: Annotated[list | None, "Updated outline"] = None,
        cta: Annotated[str | None, "Updated cta"] = None,
        keywords: Annotated[list | None, "Updated keywords"] = None,
        references: Annotated[dict | None, "Updated references"] = None,
        notes: Annotated[str | None, "Updated notes"] = None,
    ) -> dict[str, Any]:
        """Update an existing content_brief."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_brief.repository import ContentBriefRepository
            from uuid import UUID as _UUID
            repo = ContentBriefRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"ContentBrief with id '{id}' not found"}
            if objective is not None:
                entity.objective = objective
            if storytelling_framework is not None:
                entity.storytelling_framework = storytelling_framework
            if hook_options is not None:
                entity.hook_options = hook_options
            if outline is not None:
                entity.outline = outline
            if cta is not None:
                entity.cta = cta
            if keywords is not None:
                entity.keywords = keywords
            if references is not None:
                entity.references = references
            if notes is not None:
                entity.notes = notes
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_content_brief(entity)

    @mcp.tool()
    async def delete_content_brief(
        id: Annotated[str, "The UUID of the content_brief to delete"],
    ) -> dict[str, Any]:
        """Delete a content_brief by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_brief.repository import ContentBriefRepository
            from uuid import UUID as _UUID
            repo = ContentBriefRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"ContentBrief with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
