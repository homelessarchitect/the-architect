# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_persona(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.persona.serialize import serialize_persona
    return serialize_persona(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_personas(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        brand_id: Annotated[str | None, "Filter by brand_id"] = None,
    ) -> list[dict[str, Any]]:
        """List personas with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.persona.repository import PersonaRepository
            repo = PersonaRepository(session)
            filters: dict[str, Any] = {}
            if brand_id is not None:
                from uuid import UUID as _UUID
                filters["brand_id"] = _UUID(brand_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_persona(r) for r in results]

    @mcp.tool()
    async def get_persona(
        id: Annotated[str, "The UUID of the persona to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single persona by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.persona.repository import PersonaRepository
            repo = PersonaRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"Persona with id '{id}' not found"}
        return _serialize_persona(result)

    @mcp.tool()
    async def create_persona(
        brand_id: Annotated[str, "UUID of brand"],
        name: Annotated[str, "name"],
        type: Annotated[str, "type"] = "primary",
        demographics: Annotated[dict, "age_range, roles[], geography[]"] = {},
        pain_points: Annotated[list, "pain_points"] = [],
        goals: Annotated[list, "goals"] = [],
        content_channels: Annotated[list, "content_channels"] = [],
        content_preferences: Annotated[list, "content_preferences"] = [],
    ) -> dict[str, Any]:
        """Create a new persona."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.persona.models import Persona
            from factory.generated.aces.persona.repository import PersonaRepository
            from uuid import UUID as _UUID
            entity = Persona(
                brand_id=_UUID(brand_id) if brand_id is not None else None,
                name=name,
                type=type,
                demographics=demographics,
                pain_points=pain_points,
                goals=goals,
                content_channels=content_channels,
                content_preferences=content_preferences,
            )
            repo = PersonaRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_persona(result)

    @mcp.tool()
    async def update_persona(
        id: Annotated[str, "The UUID of the persona to update"],
        name: Annotated[str | None, "Updated name"] = None,
        type: Annotated[str | None, "Updated type"] = None,
        demographics: Annotated[dict | None, "Updated demographics"] = None,
        pain_points: Annotated[list | None, "Updated pain_points"] = None,
        goals: Annotated[list | None, "Updated goals"] = None,
        content_channels: Annotated[list | None, "Updated content_channels"] = None,
        content_preferences: Annotated[list | None, "Updated content_preferences"] = None,
    ) -> dict[str, Any]:
        """Update an existing persona."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.persona.repository import PersonaRepository
            from uuid import UUID as _UUID
            repo = PersonaRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"Persona with id '{id}' not found"}
            if name is not None:
                entity.name = name
            if type is not None:
                entity.type = type
            if demographics is not None:
                entity.demographics = demographics
            if pain_points is not None:
                entity.pain_points = pain_points
            if goals is not None:
                entity.goals = goals
            if content_channels is not None:
                entity.content_channels = content_channels
            if content_preferences is not None:
                entity.content_preferences = content_preferences
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_persona(entity)

    @mcp.tool()
    async def delete_persona(
        id: Annotated[str, "The UUID of the persona to delete"],
    ) -> dict[str, Any]:
        """Delete a persona by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.persona.repository import PersonaRepository
            from uuid import UUID as _UUID
            repo = PersonaRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"Persona with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
