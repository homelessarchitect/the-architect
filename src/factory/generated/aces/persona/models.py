# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

import uuid
from datetime import datetime
from enum import StrEnum

from sqlalchemy import (
    DateTime,
    String,
    ForeignKey,
    Index,
    func,
)
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from factory.core.database import Base



class PersonaType(StrEnum):
    PRIMARY = "primary"
    SECONDARY = "secondary"
    NEGATIVE = "negative"


class Persona(Base):
    __tablename__ = "aces_personas"

    id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    brand_id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_brands.id", ondelete="CASCADE"), nullable=False)
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    type: Mapped[str] = mapped_column(String(50), nullable=False, default=PersonaType.PRIMARY.value)
    demographics: Mapped[dict] = mapped_column(JSONB, nullable=False)
    pain_points: Mapped[list] = mapped_column(JSONB, nullable=False)
    goals: Mapped[list] = mapped_column(JSONB, nullable=False)
    content_channels: Mapped[list] = mapped_column(JSONB, nullable=False)
    content_preferences: Mapped[list] = mapped_column(JSONB, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        Index("ix_aces_personas_brand_id", "brand_id"),
    )
