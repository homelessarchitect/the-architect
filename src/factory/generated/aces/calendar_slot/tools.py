# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_calendar_slot(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.calendar_slot.serialize import serialize_calendar_slot
    return serialize_calendar_slot(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_calendar_slots(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        calendar_id: Annotated[str | None, "Filter by calendar_id"] = None,
        planned_date: Annotated[str | None, "Filter by planned_date"] = None,
        piece_id: Annotated[str | None, "Filter by piece_id"] = None,
    ) -> list[dict[str, Any]]:
        """List calendar_slots with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.calendar_slot.repository import CalendarSlotRepository
            repo = CalendarSlotRepository(session)
            filters: dict[str, Any] = {}
            if calendar_id is not None:
                from uuid import UUID as _UUID
                filters["calendar_id"] = _UUID(calendar_id)
            if planned_date is not None:
                filters["planned_date"] = planned_date
            if piece_id is not None:
                from uuid import UUID as _UUID
                filters["piece_id"] = _UUID(piece_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_calendar_slot(r) for r in results]

    @mcp.tool()
    async def get_calendar_slot(
        id: Annotated[str, "The UUID of the calendar_slot to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single calendar_slot by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.calendar_slot.repository import CalendarSlotRepository
            repo = CalendarSlotRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"CalendarSlot with id '{id}' not found"}
        return _serialize_calendar_slot(result)

    @mcp.tool()
    async def create_calendar_slot(
        calendar_id: Annotated[str, "UUID of calendar"],
        planned_date: Annotated[str, "ISO date YYYY-MM-DD"],
        platform: Annotated[str, "platform"],
        format: Annotated[str, "format"],
        pillar: Annotated[str, "pillar"],
        piece_id: Annotated[str | None, "UUID of piece"] = None,
        status: Annotated[str, "status"] = "planned",
    ) -> dict[str, Any]:
        """Create a new calendar_slot."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.calendar_slot.models import CalendarSlot
            from factory.generated.aces.calendar_slot.repository import CalendarSlotRepository
            from uuid import UUID as _UUID
            entity = CalendarSlot(
                calendar_id=_UUID(calendar_id) if calendar_id is not None else None,
                piece_id=_UUID(piece_id) if piece_id is not None else None,
                planned_date=planned_date,
                platform=platform,
                format=format,
                pillar=pillar,
                status=status,
            )
            repo = CalendarSlotRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_calendar_slot(result)

    @mcp.tool()
    async def update_calendar_slot(
        id: Annotated[str, "The UUID of the calendar_slot to update"],
        planned_date: Annotated[str | None, "Updated planned_date"] = None,
        platform: Annotated[str | None, "Updated platform"] = None,
        format: Annotated[str | None, "Updated format"] = None,
        pillar: Annotated[str | None, "Updated pillar"] = None,
        status: Annotated[str | None, "Updated status"] = None,
    ) -> dict[str, Any]:
        """Update an existing calendar_slot."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.calendar_slot.repository import CalendarSlotRepository
            from uuid import UUID as _UUID
            repo = CalendarSlotRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"CalendarSlot with id '{id}' not found"}
            if planned_date is not None:
                entity.planned_date = planned_date
            if platform is not None:
                entity.platform = platform
            if format is not None:
                entity.format = format
            if pillar is not None:
                entity.pillar = pillar
            if status is not None:
                entity.status = status
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_calendar_slot(entity)

    @mcp.tool()
    async def delete_calendar_slot(
        id: Annotated[str, "The UUID of the calendar_slot to delete"],
    ) -> dict[str, Any]:
        """Delete a calendar_slot by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.calendar_slot.repository import CalendarSlotRepository
            from uuid import UUID as _UUID
            repo = CalendarSlotRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"CalendarSlot with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
