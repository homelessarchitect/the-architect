# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from datetime import datetime
from typing import Annotated, Any
from uuid import UUID


def _serialize_render_job(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.render_job.serialize import serialize_render_job
    return serialize_render_job(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_render_jobs(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        asset_id: Annotated[str | None, "Filter by asset_id"] = None,
        piece_id: Annotated[str | None, "Filter by piece_id"] = None,
        script_id: Annotated[str | None, "Filter by script_id"] = None,
    ) -> list[dict[str, Any]]:
        """List render_jobs with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.render_job.repository import RenderJobRepository
            repo = RenderJobRepository(session)
            filters: dict[str, Any] = {}
            if asset_id is not None:
                from uuid import UUID as _UUID
                filters["asset_id"] = _UUID(asset_id)
            if piece_id is not None:
                from uuid import UUID as _UUID
                filters["piece_id"] = _UUID(piece_id)
            if script_id is not None:
                from uuid import UUID as _UUID
                filters["script_id"] = _UUID(script_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_render_job(r) for r in results]

    @mcp.tool()
    async def get_render_job(
        id: Annotated[str, "The UUID of the render_job to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single render_job by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.render_job.repository import RenderJobRepository
            repo = RenderJobRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"RenderJob with id '{id}' not found"}
        return _serialize_render_job(result)

    @mcp.tool()
    async def create_render_job(
        piece_id: Annotated[str, "UUID of piece"],
        script_id: Annotated[str | None, "UUID of script"] = None,
        asset_id: Annotated[str | None, "UUID of asset"] = None,
        slot_order: Annotated[int | None, "slot_order"] = None,
        status: Annotated[str, "status"] = "queued",
        output_path: Annotated[str | None, "output_path"] = None,
        video_url: Annotated[str | None, "video_url"] = None,
        error: Annotated[str | None, "error"] = None,
        started_at: Annotated[datetime | None, "started_at"] = None,
        finished_at: Annotated[datetime | None, "finished_at"] = None,
    ) -> dict[str, Any]:
        """Create a new render_job."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.render_job.models import RenderJob
            from factory.generated.aces.render_job.repository import RenderJobRepository
            from uuid import UUID as _UUID
            entity = RenderJob(
                piece_id=_UUID(piece_id) if piece_id is not None else None,
                script_id=_UUID(script_id) if script_id is not None else None,
                asset_id=_UUID(asset_id) if asset_id is not None else None,
                slot_order=slot_order,
                status=status,
                output_path=output_path,
                video_url=video_url,
                error=error,
                started_at=started_at,
                finished_at=finished_at,
            )
            repo = RenderJobRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_render_job(result)

    @mcp.tool()
    async def update_render_job(
        id: Annotated[str, "The UUID of the render_job to update"],
        slot_order: Annotated[int | None, "Updated slot_order"] = None,
        status: Annotated[str | None, "Updated status"] = None,
        output_path: Annotated[str | None, "Updated output_path"] = None,
        video_url: Annotated[str | None, "Updated video_url"] = None,
        error: Annotated[str | None, "Updated error"] = None,
        started_at: Annotated[datetime | None, "Updated started_at"] = None,
        finished_at: Annotated[datetime | None, "Updated finished_at"] = None,
    ) -> dict[str, Any]:
        """Update an existing render_job."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.render_job.repository import RenderJobRepository
            from uuid import UUID as _UUID
            repo = RenderJobRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"RenderJob with id '{id}' not found"}
            if slot_order is not None:
                entity.slot_order = slot_order
            if status is not None:
                entity.status = status
            if output_path is not None:
                entity.output_path = output_path
            if video_url is not None:
                entity.video_url = video_url
            if error is not None:
                entity.error = error
            if started_at is not None:
                entity.started_at = started_at
            if finished_at is not None:
                entity.finished_at = finished_at
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_render_job(entity)

    @mcp.tool()
    async def delete_render_job(
        id: Annotated[str, "The UUID of the render_job to delete"],
    ) -> dict[str, Any]:
        """Delete a render_job by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.render_job.repository import RenderJobRepository
            from uuid import UUID as _UUID
            repo = RenderJobRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"RenderJob with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
