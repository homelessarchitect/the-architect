# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_asset(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.asset.serialize import serialize_asset
    return serialize_asset(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_assets(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        piece_id: Annotated[str | None, "Filter by piece_id"] = None,
    ) -> list[dict[str, Any]]:
        """List assets with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.asset.repository import AssetRepository
            repo = AssetRepository(session)
            filters: dict[str, Any] = {}
            if piece_id is not None:
                from uuid import UUID as _UUID
                filters["piece_id"] = _UUID(piece_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_asset(r) for r in results]

    @mcp.tool()
    async def get_asset(
        id: Annotated[str, "The UUID of the asset to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single asset by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.asset.repository import AssetRepository
            repo = AssetRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"Asset with id '{id}' not found"}
        return _serialize_asset(result)

    @mcp.tool()
    async def create_asset(
        piece_id: Annotated[str, "UUID of piece"],
        type: Annotated[str, "type"] = "image",
        format_specs: Annotated[dict, "dimensions, aspect_ratio, duration_seconds, file_format"] = {},
        production_brief: Annotated[str | None, "production_brief"] = None,
        ai_prompt: Annotated[str | None, "ai_prompt"] = None,
        file_url: Annotated[str | None, "file_url"] = None,
        status: Annotated[str, "status"] = "pending",
    ) -> dict[str, Any]:
        """Create a new asset."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.asset.models import Asset
            from factory.generated.aces.asset.repository import AssetRepository
            from uuid import UUID as _UUID
            entity = Asset(
                piece_id=_UUID(piece_id) if piece_id is not None else None,
                type=type,
                format_specs=format_specs,
                production_brief=production_brief,
                ai_prompt=ai_prompt,
                file_url=file_url,
                status=status,
            )
            repo = AssetRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_asset(result)

    @mcp.tool()
    async def update_asset(
        id: Annotated[str, "The UUID of the asset to update"],
        type: Annotated[str | None, "Updated type"] = None,
        format_specs: Annotated[dict | None, "Updated format_specs"] = None,
        production_brief: Annotated[str | None, "Updated production_brief"] = None,
        ai_prompt: Annotated[str | None, "Updated ai_prompt"] = None,
        file_url: Annotated[str | None, "Updated file_url"] = None,
        status: Annotated[str | None, "Updated status"] = None,
    ) -> dict[str, Any]:
        """Update an existing asset."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.asset.repository import AssetRepository
            from uuid import UUID as _UUID
            repo = AssetRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"Asset with id '{id}' not found"}
            if type is not None:
                entity.type = type
            if format_specs is not None:
                entity.format_specs = format_specs
            if production_brief is not None:
                entity.production_brief = production_brief
            if ai_prompt is not None:
                entity.ai_prompt = ai_prompt
            if file_url is not None:
                entity.file_url = file_url
            if status is not None:
                entity.status = status
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_asset(entity)

    @mcp.tool()
    async def delete_asset(
        id: Annotated[str, "The UUID of the asset to delete"],
    ) -> dict[str, Any]:
        """Delete a asset by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.asset.repository import AssetRepository
            from uuid import UUID as _UUID
            repo = AssetRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"Asset with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
