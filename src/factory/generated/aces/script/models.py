# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

import uuid
from datetime import datetime
from enum import StrEnum

from sqlalchemy import (
    DateTime,
    Integer,
    String,
    Text,
    ForeignKey,
    Index,
    func,
)
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from factory.core.database import Base



class ScriptArchetype(StrEnum):
    HOW_TO = "how-to"
    MYTH_BUSTER = "myth-buster"
    CASE_STUDY = "case-study"
    SECRET_TOOL = "secret-tool"
    MISTAKE_HOOK = "mistake-hook"
    POV = "pov"
    TREND_ALERT = "trend-alert"
    ZERO_CLICK_VALUE = "zero-click-value"


class Script(Base):
    __tablename__ = "aces_scripts"

    id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    piece_id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_content_pieces.id", ondelete="CASCADE"), nullable=False)
    archetype: Mapped[str] = mapped_column(String(50), nullable=False, default=ScriptArchetype.HOW_TO.value)
    hook: Mapped[str] = mapped_column(Text, nullable=False)
    body_beats: Mapped[list] = mapped_column(JSONB, nullable=False)
    cta: Mapped[str] = mapped_column(Text, nullable=False)
    estimated_duration: Mapped[int | None] = mapped_column(Integer, nullable=True)
    platform_notes: Mapped[str | None] = mapped_column(Text, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        Index("ix_aces_scripts_piece_id", "piece_id"),
    )
