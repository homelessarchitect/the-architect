# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_script(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.script.serialize import serialize_script
    return serialize_script(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_scripts(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        piece_id: Annotated[str | None, "Filter by piece_id"] = None,
    ) -> list[dict[str, Any]]:
        """List scripts with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.script.repository import ScriptRepository
            repo = ScriptRepository(session)
            filters: dict[str, Any] = {}
            if piece_id is not None:
                from uuid import UUID as _UUID
                filters["piece_id"] = _UUID(piece_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_script(r) for r in results]

    @mcp.tool()
    async def get_script(
        id: Annotated[str, "The UUID of the script to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single script by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.script.repository import ScriptRepository
            repo = ScriptRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"Script with id '{id}' not found"}
        return _serialize_script(result)

    @mcp.tool()
    async def create_script(
        piece_id: Annotated[str, "UUID of piece"],
        hook: Annotated[str, "Opening hook -- first 3 seconds"],
        cta: Annotated[str, "Low-friction closing CTA"],
        archetype: Annotated[str, "archetype"] = "how-to",
        body_beats: Annotated[list, "[{order, beat_type, content, duration_seconds}]"] = [],
        estimated_duration: Annotated[int | None, "estimated_duration"] = None,
        platform_notes: Annotated[str | None, "platform_notes"] = None,
    ) -> dict[str, Any]:
        """Create a new script."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.script.models import Script
            from factory.generated.aces.script.repository import ScriptRepository
            from uuid import UUID as _UUID
            entity = Script(
                piece_id=_UUID(piece_id) if piece_id is not None else None,
                archetype=archetype,
                hook=hook,
                body_beats=body_beats,
                cta=cta,
                estimated_duration=estimated_duration,
                platform_notes=platform_notes,
            )
            repo = ScriptRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_script(result)

    @mcp.tool()
    async def update_script(
        id: Annotated[str, "The UUID of the script to update"],
        archetype: Annotated[str | None, "Updated archetype"] = None,
        hook: Annotated[str | None, "Updated hook"] = None,
        body_beats: Annotated[list | None, "Updated body_beats"] = None,
        cta: Annotated[str | None, "Updated cta"] = None,
        estimated_duration: Annotated[int | None, "Updated estimated_duration"] = None,
        platform_notes: Annotated[str | None, "Updated platform_notes"] = None,
    ) -> dict[str, Any]:
        """Update an existing script."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.script.repository import ScriptRepository
            from uuid import UUID as _UUID
            repo = ScriptRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"Script with id '{id}' not found"}
            if archetype is not None:
                entity.archetype = archetype
            if hook is not None:
                entity.hook = hook
            if body_beats is not None:
                entity.body_beats = body_beats
            if cta is not None:
                entity.cta = cta
            if estimated_duration is not None:
                entity.estimated_duration = estimated_duration
            if platform_notes is not None:
                entity.platform_notes = platform_notes
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_script(entity)

    @mcp.tool()
    async def delete_script(
        id: Annotated[str, "The UUID of the script to delete"],
    ) -> dict[str, Any]:
        """Delete a script by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.script.repository import ScriptRepository
            from uuid import UUID as _UUID
            repo = ScriptRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"Script with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
