# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

import uuid
from datetime import datetime
from enum import StrEnum

from sqlalchemy import (
    DateTime,
    String,
    Text,
    ForeignKey,
    Index,
    func,
)
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from factory.core.database import Base



class IdeaSource(StrEnum):
    MANUAL = "manual"
    AI_GENERATED = "ai-generated"
    TRENDING = "trending"


class IdeaStatus(StrEnum):
    BACKLOG = "backlog"
    BRIEFED = "briefed"
    IN_PRODUCTION = "in-production"
    PUBLISHED = "published"
    DISCARDED = "discarded"


class ContentIdea(Base):
    __tablename__ = "aces_content_ideas"

    id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    brand_id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_brands.id", ondelete="CASCADE"), nullable=False)
    persona_id: Mapped[uuid.UUID | None] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_personas.id", ondelete="CASCADE"), nullable=True)
    pillar: Mapped[str] = mapped_column(String(100), nullable=False)
    concept: Mapped[str] = mapped_column(Text, nullable=False)
    angle: Mapped[str] = mapped_column(Text, nullable=False)
    formats: Mapped[list] = mapped_column(JSONB, nullable=False)
    source: Mapped[str] = mapped_column(String(50), nullable=False, default=IdeaSource.MANUAL.value)
    status: Mapped[str] = mapped_column(String(50), nullable=False, default=IdeaStatus.BACKLOG.value)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        Index("ix_aces_content_ideas_brand_id", "brand_id"),
        Index("ix_aces_content_ideas_status", "status"),
    )
