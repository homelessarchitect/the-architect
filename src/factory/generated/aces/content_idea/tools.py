# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_content_idea(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.content_idea.serialize import serialize_content_idea
    return serialize_content_idea(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_content_ideas(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        brand_id: Annotated[str | None, "Filter by brand_id"] = None,
        status: Annotated[str | None, "Filter by status"] = None,
        persona_id: Annotated[str | None, "Filter by persona_id"] = None,
    ) -> list[dict[str, Any]]:
        """List content_ideas with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_idea.repository import ContentIdeaRepository
            repo = ContentIdeaRepository(session)
            filters: dict[str, Any] = {}
            if brand_id is not None:
                from uuid import UUID as _UUID
                filters["brand_id"] = _UUID(brand_id)
            if status is not None:
                filters["status"] = status
            if persona_id is not None:
                from uuid import UUID as _UUID
                filters["persona_id"] = _UUID(persona_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_content_idea(r) for r in results]

    @mcp.tool()
    async def get_content_idea(
        id: Annotated[str, "The UUID of the content_idea to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single content_idea by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_idea.repository import ContentIdeaRepository
            repo = ContentIdeaRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"ContentIdea with id '{id}' not found"}
        return _serialize_content_idea(result)

    @mcp.tool()
    async def create_content_idea(
        brand_id: Annotated[str, "UUID of brand"],
        pillar: Annotated[str, "Must match Brand.content_pillars"],
        concept: Annotated[str, "One-sentence idea"],
        angle: Annotated[str, "Specific take/hook direction"],
        persona_id: Annotated[str | None, "UUID of persona"] = None,
        formats: Annotated[list, "formats"] = [],
        source: Annotated[str, "source"] = "manual",
        status: Annotated[str, "status"] = "backlog",
    ) -> dict[str, Any]:
        """Create a new content_idea."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_idea.models import ContentIdea
            from factory.generated.aces.content_idea.repository import ContentIdeaRepository
            from uuid import UUID as _UUID
            entity = ContentIdea(
                brand_id=_UUID(brand_id) if brand_id is not None else None,
                persona_id=_UUID(persona_id) if persona_id is not None else None,
                pillar=pillar,
                concept=concept,
                angle=angle,
                formats=formats,
                source=source,
                status=status,
            )
            repo = ContentIdeaRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_content_idea(result)

    @mcp.tool()
    async def update_content_idea(
        id: Annotated[str, "The UUID of the content_idea to update"],
        pillar: Annotated[str | None, "Updated pillar"] = None,
        concept: Annotated[str | None, "Updated concept"] = None,
        angle: Annotated[str | None, "Updated angle"] = None,
        formats: Annotated[list | None, "Updated formats"] = None,
        source: Annotated[str | None, "Updated source"] = None,
        status: Annotated[str | None, "Updated status"] = None,
    ) -> dict[str, Any]:
        """Update an existing content_idea."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_idea.repository import ContentIdeaRepository
            from uuid import UUID as _UUID
            repo = ContentIdeaRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"ContentIdea with id '{id}' not found"}
            if pillar is not None:
                entity.pillar = pillar
            if concept is not None:
                entity.concept = concept
            if angle is not None:
                entity.angle = angle
            if formats is not None:
                entity.formats = formats
            if source is not None:
                entity.source = source
            if status is not None:
                entity.status = status
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_content_idea(entity)

    @mcp.tool()
    async def delete_content_idea(
        id: Annotated[str, "The UUID of the content_idea to delete"],
    ) -> dict[str, Any]:
        """Delete a content_idea by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_idea.repository import ContentIdeaRepository
            from uuid import UUID as _UUID
            repo = ContentIdeaRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"ContentIdea with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
