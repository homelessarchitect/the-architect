# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_slide_plan(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.slide_plan.serialize import serialize_slide_plan
    return serialize_slide_plan(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_slide_plans(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        piece_id: Annotated[str | None, "Filter by piece_id"] = None,
    ) -> list[dict[str, Any]]:
        """List slide_plans with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.slide_plan.repository import SlidePlanRepository
            repo = SlidePlanRepository(session)
            filters: dict[str, Any] = {}
            if piece_id is not None:
                from uuid import UUID as _UUID
                filters["piece_id"] = _UUID(piece_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_slide_plan(r) for r in results]

    @mcp.tool()
    async def get_slide_plan(
        id: Annotated[str, "The UUID of the slide_plan to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single slide_plan by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.slide_plan.repository import SlidePlanRepository
            repo = SlidePlanRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"SlidePlan with id '{id}' not found"}
        return _serialize_slide_plan(result)

    @mcp.tool()
    async def create_slide_plan(
        piece_id: Annotated[str, "UUID of piece"],
        slots: Annotated[list, "[{order, role, text_overlay, visual_brief, suggested_archetype}]"] = [],
    ) -> dict[str, Any]:
        """Create a new slide_plan."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.slide_plan.models import SlidePlan
            from factory.generated.aces.slide_plan.repository import SlidePlanRepository
            from uuid import UUID as _UUID
            entity = SlidePlan(
                piece_id=_UUID(piece_id) if piece_id is not None else None,
                slots=slots,
            )
            repo = SlidePlanRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_slide_plan(result)

    @mcp.tool()
    async def update_slide_plan(
        id: Annotated[str, "The UUID of the slide_plan to update"],
        slots: Annotated[list | None, "Updated slots"] = None,
    ) -> dict[str, Any]:
        """Update an existing slide_plan."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.slide_plan.repository import SlidePlanRepository
            from uuid import UUID as _UUID
            repo = SlidePlanRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"SlidePlan with id '{id}' not found"}
            if slots is not None:
                entity.slots = slots
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_slide_plan(entity)

    @mcp.tool()
    async def delete_slide_plan(
        id: Annotated[str, "The UUID of the slide_plan to delete"],
    ) -> dict[str, Any]:
        """Delete a slide_plan by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.slide_plan.repository import SlidePlanRepository
            from uuid import UUID as _UUID
            repo = SlidePlanRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"SlidePlan with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
