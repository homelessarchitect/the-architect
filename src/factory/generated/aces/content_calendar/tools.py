# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_content_calendar(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.content_calendar.serialize import serialize_content_calendar
    return serialize_content_calendar(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_content_calendars(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        brand_id: Annotated[str | None, "Filter by brand_id"] = None,
    ) -> list[dict[str, Any]]:
        """List content_calendars with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_calendar.repository import ContentCalendarRepository
            repo = ContentCalendarRepository(session)
            filters: dict[str, Any] = {}
            if brand_id is not None:
                from uuid import UUID as _UUID
                filters["brand_id"] = _UUID(brand_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_content_calendar(r) for r in results]

    @mcp.tool()
    async def get_content_calendar(
        id: Annotated[str, "The UUID of the content_calendar to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single content_calendar by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_calendar.repository import ContentCalendarRepository
            repo = ContentCalendarRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"ContentCalendar with id '{id}' not found"}
        return _serialize_content_calendar(result)

    @mcp.tool()
    async def create_content_calendar(
        brand_id: Annotated[str, "UUID of brand"],
        name: Annotated[str, "name"],
        editorial_theme: Annotated[str, "Macro narrative for this period"],
        period_start: Annotated[str, "ISO date YYYY-MM-DD"],
        period_end: Annotated[str, "ISO date YYYY-MM-DD"],
        platform_frequency: Annotated[dict, "{instagram: 5, tiktok: 7, newsletter: 1} per week"] = {},
        status: Annotated[str, "status"] = "draft",
    ) -> dict[str, Any]:
        """Create a new content_calendar."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_calendar.models import ContentCalendar
            from factory.generated.aces.content_calendar.repository import ContentCalendarRepository
            from uuid import UUID as _UUID
            entity = ContentCalendar(
                brand_id=_UUID(brand_id) if brand_id is not None else None,
                name=name,
                editorial_theme=editorial_theme,
                period_start=period_start,
                period_end=period_end,
                platform_frequency=platform_frequency,
                status=status,
            )
            repo = ContentCalendarRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_content_calendar(result)

    @mcp.tool()
    async def update_content_calendar(
        id: Annotated[str, "The UUID of the content_calendar to update"],
        name: Annotated[str | None, "Updated name"] = None,
        editorial_theme: Annotated[str | None, "Updated editorial_theme"] = None,
        period_start: Annotated[str | None, "Updated period_start"] = None,
        period_end: Annotated[str | None, "Updated period_end"] = None,
        platform_frequency: Annotated[dict | None, "Updated platform_frequency"] = None,
        status: Annotated[str | None, "Updated status"] = None,
    ) -> dict[str, Any]:
        """Update an existing content_calendar."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_calendar.repository import ContentCalendarRepository
            from uuid import UUID as _UUID
            repo = ContentCalendarRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"ContentCalendar with id '{id}' not found"}
            if name is not None:
                entity.name = name
            if editorial_theme is not None:
                entity.editorial_theme = editorial_theme
            if period_start is not None:
                entity.period_start = period_start
            if period_end is not None:
                entity.period_end = period_end
            if platform_frequency is not None:
                entity.platform_frequency = platform_frequency
            if status is not None:
                entity.status = status
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_content_calendar(entity)

    @mcp.tool()
    async def delete_content_calendar(
        id: Annotated[str, "The UUID of the content_calendar to delete"],
    ) -> dict[str, Any]:
        """Delete a content_calendar by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_calendar.repository import ContentCalendarRepository
            from uuid import UUID as _UUID
            repo = ContentCalendarRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"ContentCalendar with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
