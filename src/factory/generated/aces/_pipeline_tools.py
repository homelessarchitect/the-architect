# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any


# Pipeline transition data (baked at generation time)
_PIPELINE_TRANSITIONS: dict[str, dict] = {
    "content_idea": {
        "statuses": ['backlog', 'briefed', 'in-production', 'published', 'discarded'],
        "initial_status": "backlog",
        "transitions": [
            {
                "from_status": "backlog",
                "to_status": "briefed",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "briefed",
                "to_status": "in-production",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "in-production",
                "to_status": "published",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "backlog",
                "to_status": "discarded",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "briefed",
                "to_status": "discarded",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "in-production",
                "to_status": "discarded",
                "approval_required": False,
                "approval_action_type": "",
            },
        ],
    },
    "content_piece": {
        "statuses": ['idea',
 'briefed',
 'scripted',
 'in-production',
 'review',
 'approved',
 'scheduled',
 'published',
 'archived'],
        "initial_status": "idea",
        "transitions": [
            {
                "from_status": "idea",
                "to_status": "briefed",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "briefed",
                "to_status": "scripted",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "briefed",
                "to_status": "in-production",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "scripted",
                "to_status": "in-production",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "in-production",
                "to_status": "review",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "review",
                "to_status": "approved",
                "approval_required": True,
                "approval_action_type": "publish_piece",
            },
            {
                "from_status": "approved",
                "to_status": "scheduled",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "approved",
                "to_status": "published",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "scheduled",
                "to_status": "published",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "published",
                "to_status": "archived",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "review",
                "to_status": "in-production",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "idea",
                "to_status": "archived",
                "approval_required": False,
                "approval_action_type": "",
            },
        ],
    },
    "asset": {
        "statuses": ['pending', 'in-progress', 'ready', 'approved'],
        "initial_status": "pending",
        "transitions": [
            {
                "from_status": "pending",
                "to_status": "in-progress",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "in-progress",
                "to_status": "ready",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "ready",
                "to_status": "approved",
                "approval_required": False,
                "approval_action_type": "",
            },
        ],
    },
    "render_job": {
        "statuses": ['queued', 'rendering', 'ready', 'failed'],
        "initial_status": "queued",
        "transitions": [
            {
                "from_status": "queued",
                "to_status": "rendering",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "rendering",
                "to_status": "ready",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "rendering",
                "to_status": "failed",
                "approval_required": False,
                "approval_action_type": "",
            },
        ],
    },
    "content_calendar": {
        "statuses": ['draft', 'active', 'completed', 'archived'],
        "initial_status": "draft",
        "transitions": [
            {
                "from_status": "draft",
                "to_status": "active",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "active",
                "to_status": "completed",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "completed",
                "to_status": "archived",
                "approval_required": False,
                "approval_action_type": "",
            },
        ],
    },
    "calendar_slot": {
        "statuses": ['planned', 'in-production', 'ready', 'published'],
        "initial_status": "planned",
        "transitions": [
            {
                "from_status": "planned",
                "to_status": "in-production",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "in-production",
                "to_status": "ready",
                "approval_required": False,
                "approval_action_type": "",
            },
            {
                "from_status": "ready",
                "to_status": "published",
                "approval_required": False,
                "approval_action_type": "",
            },
        ],
    },
}


def _validate_transition(
    entity_name: str, from_status: str, to_status: str
) -> dict[str, Any]:
    """Validate a status transition using baked pipeline data."""
    pipeline = _PIPELINE_TRANSITIONS.get(entity_name)
    if pipeline is None:
        return {"allowed": True}

    if from_status not in pipeline["statuses"]:
        return {
            "allowed": False,
            "error": f"Current status '{from_status}' is not valid for entity '{entity_name}'",
        }

    if to_status not in pipeline["statuses"]:
        return {
            "allowed": False,
            "error": f"Target status '{to_status}' is not valid for entity '{entity_name}'",
        }

    allowed_targets: list[str] = []
    matching = None

    for t in pipeline["transitions"]:
        if t["from_status"] == from_status:
            allowed_targets.append(t["to_status"])
            if t["to_status"] == to_status:
                matching = t

    if matching is None:
        if not allowed_targets:
            return {
                "allowed": False,
                "error": f"No transitions defined from '{from_status}'",
                "allowed_transitions": [],
            }
        return {
            "allowed": False,
            "error": f"Cannot transition from '{from_status}' to '{to_status}'",
            "allowed_transitions": allowed_targets,
        }

    if matching["approval_required"]:
        return {
            "allowed": True,
            "approval_required": True,
            "approval_action_type": matching["approval_action_type"],
        }

    return {"allowed": True}


def register(mcp: Any, workflow_slug: str = "aces") -> None:

    @mcp.tool()
    async def transition_content_idea_status(
        id: Annotated[str, "The UUID of the content_idea to transition"],
        to_status: Annotated[str, "Target status. Valid statuses: backlog, briefed, in-production, published, discarded"],
    ) -> dict[str, Any]:
        """Transition a content_idea to a new status.

        Validates the transition is allowed by the pipeline definition.
        If the transition requires approval, creates an Approval record instead.
        """
        from uuid import UUID as _UUID

        from factory.core.database import get_session_factory
        from factory.generated.aces.content_idea.repository import ContentIdeaRepository

        factory = get_session_factory()
        async with factory() as session:
            repo = ContentIdeaRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"content_idea with id '{id}' not found"}

            current_status = entity.status
            result = _validate_transition("content_idea", current_status, to_status)

            if not result.get("allowed"):
                return {
                    "error": result.get("error", "Transition not allowed"),
                    "current_status": current_status,
                    "allowed_transitions": result.get("allowed_transitions"),
                }

            if result.get("approval_required"):
                from factory.modules.approvals.models import Approval  # noqa: F401
                from factory.modules.approvals.service import ApprovalService

                approval_svc = ApprovalService(session)
                approval = await approval_svc.create(
                    workflow_slug=workflow_slug,
                    action_type=result["approval_action_type"],
                    payload={
                        "entity_name": "content_idea",
                        "entity_id": id,
                        "from_status": current_status,
                        "to_status": to_status,
                    },
                    related_entity_id=_UUID(id),
                    requested_by="agent",
                )
                await session.commit()
                return {
                    "status": "approval_required",
                    "approval_id": str(approval.id),
                    "action_type": result["approval_action_type"],
                    "message": (
                        f"Transition from '{current_status}' to '{to_status}' requires approval. "
                        f"Approval '{approval.id}' created."
                    ),
                }

            entity.status = to_status
            await session.flush()
            await session.refresh(entity)
            await session.commit()

            from factory.generated.aces.content_idea.serialize import serialize_content_idea
            return {
                "status": "transitioned",
                "from_status": current_status,
                "to_status": to_status,
                "entity": serialize_content_idea(entity),
            }

    @mcp.tool()
    async def transition_content_piece_status(
        id: Annotated[str, "The UUID of the content_piece to transition"],
        to_status: Annotated[str, "Target status. Valid statuses: idea, briefed, scripted, in-production, review, approved, scheduled, published, archived"],
    ) -> dict[str, Any]:
        """Transition a content_piece to a new status.

        Validates the transition is allowed by the pipeline definition.
        If the transition requires approval, creates an Approval record instead.
        """
        from uuid import UUID as _UUID

        from factory.core.database import get_session_factory
        from factory.generated.aces.content_piece.repository import ContentPieceRepository

        factory = get_session_factory()
        async with factory() as session:
            repo = ContentPieceRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"content_piece with id '{id}' not found"}

            current_status = entity.status
            result = _validate_transition("content_piece", current_status, to_status)

            if not result.get("allowed"):
                return {
                    "error": result.get("error", "Transition not allowed"),
                    "current_status": current_status,
                    "allowed_transitions": result.get("allowed_transitions"),
                }

            if result.get("approval_required"):
                from factory.modules.approvals.models import Approval  # noqa: F401
                from factory.modules.approvals.service import ApprovalService

                approval_svc = ApprovalService(session)
                approval = await approval_svc.create(
                    workflow_slug=workflow_slug,
                    action_type=result["approval_action_type"],
                    payload={
                        "entity_name": "content_piece",
                        "entity_id": id,
                        "from_status": current_status,
                        "to_status": to_status,
                    },
                    related_entity_id=_UUID(id),
                    requested_by="agent",
                )
                await session.commit()
                return {
                    "status": "approval_required",
                    "approval_id": str(approval.id),
                    "action_type": result["approval_action_type"],
                    "message": (
                        f"Transition from '{current_status}' to '{to_status}' requires approval. "
                        f"Approval '{approval.id}' created."
                    ),
                }

            entity.status = to_status
            await session.flush()
            await session.refresh(entity)
            await session.commit()

            from factory.generated.aces.content_piece.serialize import serialize_content_piece
            return {
                "status": "transitioned",
                "from_status": current_status,
                "to_status": to_status,
                "entity": serialize_content_piece(entity),
            }

    @mcp.tool()
    async def transition_asset_status(
        id: Annotated[str, "The UUID of the asset to transition"],
        to_status: Annotated[str, "Target status. Valid statuses: pending, in-progress, ready, approved"],
    ) -> dict[str, Any]:
        """Transition a asset to a new status.

        Validates the transition is allowed by the pipeline definition.
        If the transition requires approval, creates an Approval record instead.
        """
        from uuid import UUID as _UUID

        from factory.core.database import get_session_factory
        from factory.generated.aces.asset.repository import AssetRepository

        factory = get_session_factory()
        async with factory() as session:
            repo = AssetRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"asset with id '{id}' not found"}

            current_status = entity.status
            result = _validate_transition("asset", current_status, to_status)

            if not result.get("allowed"):
                return {
                    "error": result.get("error", "Transition not allowed"),
                    "current_status": current_status,
                    "allowed_transitions": result.get("allowed_transitions"),
                }

            if result.get("approval_required"):
                from factory.modules.approvals.models import Approval  # noqa: F401
                from factory.modules.approvals.service import ApprovalService

                approval_svc = ApprovalService(session)
                approval = await approval_svc.create(
                    workflow_slug=workflow_slug,
                    action_type=result["approval_action_type"],
                    payload={
                        "entity_name": "asset",
                        "entity_id": id,
                        "from_status": current_status,
                        "to_status": to_status,
                    },
                    related_entity_id=_UUID(id),
                    requested_by="agent",
                )
                await session.commit()
                return {
                    "status": "approval_required",
                    "approval_id": str(approval.id),
                    "action_type": result["approval_action_type"],
                    "message": (
                        f"Transition from '{current_status}' to '{to_status}' requires approval. "
                        f"Approval '{approval.id}' created."
                    ),
                }

            entity.status = to_status
            await session.flush()
            await session.refresh(entity)
            await session.commit()

            from factory.generated.aces.asset.serialize import serialize_asset
            return {
                "status": "transitioned",
                "from_status": current_status,
                "to_status": to_status,
                "entity": serialize_asset(entity),
            }

    @mcp.tool()
    async def transition_render_job_status(
        id: Annotated[str, "The UUID of the render_job to transition"],
        to_status: Annotated[str, "Target status. Valid statuses: queued, rendering, ready, failed"],
    ) -> dict[str, Any]:
        """Transition a render_job to a new status.

        Validates the transition is allowed by the pipeline definition.
        If the transition requires approval, creates an Approval record instead.
        """
        from uuid import UUID as _UUID

        from factory.core.database import get_session_factory
        from factory.generated.aces.render_job.repository import RenderJobRepository

        factory = get_session_factory()
        async with factory() as session:
            repo = RenderJobRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"render_job with id '{id}' not found"}

            current_status = entity.status
            result = _validate_transition("render_job", current_status, to_status)

            if not result.get("allowed"):
                return {
                    "error": result.get("error", "Transition not allowed"),
                    "current_status": current_status,
                    "allowed_transitions": result.get("allowed_transitions"),
                }

            if result.get("approval_required"):
                from factory.modules.approvals.models import Approval  # noqa: F401
                from factory.modules.approvals.service import ApprovalService

                approval_svc = ApprovalService(session)
                approval = await approval_svc.create(
                    workflow_slug=workflow_slug,
                    action_type=result["approval_action_type"],
                    payload={
                        "entity_name": "render_job",
                        "entity_id": id,
                        "from_status": current_status,
                        "to_status": to_status,
                    },
                    related_entity_id=_UUID(id),
                    requested_by="agent",
                )
                await session.commit()
                return {
                    "status": "approval_required",
                    "approval_id": str(approval.id),
                    "action_type": result["approval_action_type"],
                    "message": (
                        f"Transition from '{current_status}' to '{to_status}' requires approval. "
                        f"Approval '{approval.id}' created."
                    ),
                }

            entity.status = to_status
            await session.flush()
            await session.refresh(entity)
            await session.commit()

            from factory.generated.aces.render_job.serialize import serialize_render_job
            return {
                "status": "transitioned",
                "from_status": current_status,
                "to_status": to_status,
                "entity": serialize_render_job(entity),
            }

    @mcp.tool()
    async def transition_content_calendar_status(
        id: Annotated[str, "The UUID of the content_calendar to transition"],
        to_status: Annotated[str, "Target status. Valid statuses: draft, active, completed, archived"],
    ) -> dict[str, Any]:
        """Transition a content_calendar to a new status.

        Validates the transition is allowed by the pipeline definition.
        If the transition requires approval, creates an Approval record instead.
        """
        from uuid import UUID as _UUID

        from factory.core.database import get_session_factory
        from factory.generated.aces.content_calendar.repository import ContentCalendarRepository

        factory = get_session_factory()
        async with factory() as session:
            repo = ContentCalendarRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"content_calendar with id '{id}' not found"}

            current_status = entity.status
            result = _validate_transition("content_calendar", current_status, to_status)

            if not result.get("allowed"):
                return {
                    "error": result.get("error", "Transition not allowed"),
                    "current_status": current_status,
                    "allowed_transitions": result.get("allowed_transitions"),
                }

            if result.get("approval_required"):
                from factory.modules.approvals.models import Approval  # noqa: F401
                from factory.modules.approvals.service import ApprovalService

                approval_svc = ApprovalService(session)
                approval = await approval_svc.create(
                    workflow_slug=workflow_slug,
                    action_type=result["approval_action_type"],
                    payload={
                        "entity_name": "content_calendar",
                        "entity_id": id,
                        "from_status": current_status,
                        "to_status": to_status,
                    },
                    related_entity_id=_UUID(id),
                    requested_by="agent",
                )
                await session.commit()
                return {
                    "status": "approval_required",
                    "approval_id": str(approval.id),
                    "action_type": result["approval_action_type"],
                    "message": (
                        f"Transition from '{current_status}' to '{to_status}' requires approval. "
                        f"Approval '{approval.id}' created."
                    ),
                }

            entity.status = to_status
            await session.flush()
            await session.refresh(entity)
            await session.commit()

            from factory.generated.aces.content_calendar.serialize import serialize_content_calendar
            return {
                "status": "transitioned",
                "from_status": current_status,
                "to_status": to_status,
                "entity": serialize_content_calendar(entity),
            }

    @mcp.tool()
    async def transition_calendar_slot_status(
        id: Annotated[str, "The UUID of the calendar_slot to transition"],
        to_status: Annotated[str, "Target status. Valid statuses: planned, in-production, ready, published"],
    ) -> dict[str, Any]:
        """Transition a calendar_slot to a new status.

        Validates the transition is allowed by the pipeline definition.
        If the transition requires approval, creates an Approval record instead.
        """
        from uuid import UUID as _UUID

        from factory.core.database import get_session_factory
        from factory.generated.aces.calendar_slot.repository import CalendarSlotRepository

        factory = get_session_factory()
        async with factory() as session:
            repo = CalendarSlotRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"calendar_slot with id '{id}' not found"}

            current_status = entity.status
            result = _validate_transition("calendar_slot", current_status, to_status)

            if not result.get("allowed"):
                return {
                    "error": result.get("error", "Transition not allowed"),
                    "current_status": current_status,
                    "allowed_transitions": result.get("allowed_transitions"),
                }

            if result.get("approval_required"):
                from factory.modules.approvals.models import Approval  # noqa: F401
                from factory.modules.approvals.service import ApprovalService

                approval_svc = ApprovalService(session)
                approval = await approval_svc.create(
                    workflow_slug=workflow_slug,
                    action_type=result["approval_action_type"],
                    payload={
                        "entity_name": "calendar_slot",
                        "entity_id": id,
                        "from_status": current_status,
                        "to_status": to_status,
                    },
                    related_entity_id=_UUID(id),
                    requested_by="agent",
                )
                await session.commit()
                return {
                    "status": "approval_required",
                    "approval_id": str(approval.id),
                    "action_type": result["approval_action_type"],
                    "message": (
                        f"Transition from '{current_status}' to '{to_status}' requires approval. "
                        f"Approval '{approval.id}' created."
                    ),
                }

            entity.status = to_status
            await session.flush()
            await session.refresh(entity)
            await session.commit()

            from factory.generated.aces.calendar_slot.serialize import serialize_calendar_slot
            return {
                "status": "transitioned",
                "from_status": current_status,
                "to_status": to_status,
                "entity": serialize_calendar_slot(entity),
            }
