# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from datetime import datetime
from typing import Annotated, Any
from uuid import UUID


def _serialize_content_metric(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.content_metric.serialize import serialize_content_metric
    return serialize_content_metric(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_content_metrics(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        published_id: Annotated[str | None, "Filter by published_id"] = None,
    ) -> list[dict[str, Any]]:
        """List content_metrics with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_metric.repository import ContentMetricRepository
            repo = ContentMetricRepository(session)
            filters: dict[str, Any] = {}
            if published_id is not None:
                from uuid import UUID as _UUID
                filters["published_id"] = _UUID(published_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_content_metric(r) for r in results]

    @mcp.tool()
    async def get_content_metric(
        id: Annotated[str, "The UUID of the content_metric to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single content_metric by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_metric.repository import ContentMetricRepository
            repo = ContentMetricRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"ContentMetric with id '{id}' not found"}
        return _serialize_content_metric(result)

    @mcp.tool()
    async def create_content_metric(
        published_id: Annotated[str, "UUID of published"],
        captured_at: Annotated[datetime, "captured_at"],
        reach: Annotated[int, "reach"] = 0,
        impressions: Annotated[int, "impressions"] = 0,
        engagement_rate: Annotated[float, "engagement_rate"] = 0.0,
        likes: Annotated[int, "likes"] = 0,
        comments: Annotated[int, "comments"] = 0,
        shares: Annotated[int, "shares"] = 0,
        saves: Annotated[int, "saves"] = 0,
        clicks: Annotated[int, "clicks"] = 0,
        completion_rate: Annotated[float | None, "completion_rate"] = None,
        conversions: Annotated[int | None, "conversions"] = None,
    ) -> dict[str, Any]:
        """Create a new content_metric."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_metric.models import ContentMetric
            from factory.generated.aces.content_metric.repository import ContentMetricRepository
            from uuid import UUID as _UUID
            entity = ContentMetric(
                published_id=_UUID(published_id) if published_id is not None else None,
                reach=reach,
                impressions=impressions,
                engagement_rate=engagement_rate,
                likes=likes,
                comments=comments,
                shares=shares,
                saves=saves,
                clicks=clicks,
                completion_rate=completion_rate,
                conversions=conversions,
                captured_at=captured_at,
            )
            repo = ContentMetricRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_content_metric(result)

    @mcp.tool()
    async def update_content_metric(
        id: Annotated[str, "The UUID of the content_metric to update"],
        reach: Annotated[int | None, "Updated reach"] = None,
        impressions: Annotated[int | None, "Updated impressions"] = None,
        engagement_rate: Annotated[float | None, "Updated engagement_rate"] = None,
        likes: Annotated[int | None, "Updated likes"] = None,
        comments: Annotated[int | None, "Updated comments"] = None,
        shares: Annotated[int | None, "Updated shares"] = None,
        saves: Annotated[int | None, "Updated saves"] = None,
        clicks: Annotated[int | None, "Updated clicks"] = None,
        completion_rate: Annotated[float | None, "Updated completion_rate"] = None,
        conversions: Annotated[int | None, "Updated conversions"] = None,
        captured_at: Annotated[datetime | None, "Updated captured_at"] = None,
    ) -> dict[str, Any]:
        """Update an existing content_metric."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_metric.repository import ContentMetricRepository
            from uuid import UUID as _UUID
            repo = ContentMetricRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"ContentMetric with id '{id}' not found"}
            if reach is not None:
                entity.reach = reach
            if impressions is not None:
                entity.impressions = impressions
            if engagement_rate is not None:
                entity.engagement_rate = engagement_rate
            if likes is not None:
                entity.likes = likes
            if comments is not None:
                entity.comments = comments
            if shares is not None:
                entity.shares = shares
            if saves is not None:
                entity.saves = saves
            if clicks is not None:
                entity.clicks = clicks
            if completion_rate is not None:
                entity.completion_rate = completion_rate
            if conversions is not None:
                entity.conversions = conversions
            if captured_at is not None:
                entity.captured_at = captured_at
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_content_metric(entity)

    @mcp.tool()
    async def delete_content_metric(
        id: Annotated[str, "The UUID of the content_metric to delete"],
    ) -> dict[str, Any]:
        """Delete a content_metric by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.content_metric.repository import ContentMetricRepository
            from uuid import UUID as _UUID
            repo = ContentMetricRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"ContentMetric with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
