# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_social_account(instance: Any) -> dict[str, Any]:
    from factory.generated.aces.social_account.serialize import serialize_social_account
    return serialize_social_account(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_social_accounts(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        brand_id: Annotated[str | None, "Filter by brand_id"] = None,
    ) -> list[dict[str, Any]]:
        """List social_accounts with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.social_account.repository import SocialAccountRepository
            repo = SocialAccountRepository(session)
            filters: dict[str, Any] = {}
            if brand_id is not None:
                from uuid import UUID as _UUID
                filters["brand_id"] = _UUID(brand_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_social_account(r) for r in results]

    @mcp.tool()
    async def get_social_account(
        id: Annotated[str, "The UUID of the social_account to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single social_account by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.social_account.repository import SocialAccountRepository
            repo = SocialAccountRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"SocialAccount with id '{id}' not found"}
        return _serialize_social_account(result)

    @mcp.tool()
    async def create_social_account(
        brand_id: Annotated[str, "UUID of brand"],
        platform: Annotated[str, "platform"],
        display_name: Annotated[str, "display_name"],
        handle: Annotated[str | None, "handle"] = None,
        is_active: Annotated[bool, "is_active"] = True,
    ) -> dict[str, Any]:
        """Create a new social_account."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.social_account.models import SocialAccount
            from factory.generated.aces.social_account.repository import SocialAccountRepository
            from uuid import UUID as _UUID
            entity = SocialAccount(
                brand_id=_UUID(brand_id) if brand_id is not None else None,
                platform=platform,
                display_name=display_name,
                handle=handle,
                is_active=is_active,
            )
            repo = SocialAccountRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_social_account(result)

    @mcp.tool()
    async def update_social_account(
        id: Annotated[str, "The UUID of the social_account to update"],
        platform: Annotated[str | None, "Updated platform"] = None,
        display_name: Annotated[str | None, "Updated display_name"] = None,
        handle: Annotated[str | None, "Updated handle"] = None,
        is_active: Annotated[bool | None, "Updated is_active"] = None,
    ) -> dict[str, Any]:
        """Update an existing social_account."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.social_account.repository import SocialAccountRepository
            from uuid import UUID as _UUID
            repo = SocialAccountRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"SocialAccount with id '{id}' not found"}
            if platform is not None:
                entity.platform = platform
            if display_name is not None:
                entity.display_name = display_name
            if handle is not None:
                entity.handle = handle
            if is_active is not None:
                entity.is_active = is_active
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_social_account(entity)

    @mcp.tool()
    async def delete_social_account(
        id: Annotated[str, "The UUID of the social_account to delete"],
    ) -> dict[str, Any]:
        """Delete a social_account by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.aces.social_account.repository import SocialAccountRepository
            from uuid import UUID as _UUID
            repo = SocialAccountRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"SocialAccount with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
