# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

import uuid
from datetime import datetime
from enum import StrEnum

from sqlalchemy import (
    DateTime,
    String,
    Text,
    ForeignKey,
    Index,
    func,
)
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from factory.core.database import Base



class ContentFormat(StrEnum):
    REEL = "reel"
    CAROUSEL = "carousel"
    STATIC_POST = "static-post"
    STORY = "story"
    THREAD = "thread"
    NEWSLETTER = "newsletter"
    LONG_VIDEO = "long-video"
    SHORT_VIDEO = "short-video"
    BLOG = "blog"
    PODCAST = "podcast"


class ContentPlatform(StrEnum):
    INSTAGRAM = "instagram"
    TIKTOK = "tiktok"
    LINKEDIN = "linkedin"
    YOUTUBE = "youtube"
    TWITTER = "twitter"
    NEWSLETTER = "newsletter"
    WEB = "web"


class PieceStatus(StrEnum):
    IDEA = "idea"
    BRIEFED = "briefed"
    SCRIPTED = "scripted"
    IN_PRODUCTION = "in-production"
    REVIEW = "review"
    APPROVED = "approved"
    SCHEDULED = "scheduled"
    PUBLISHED = "published"
    ARCHIVED = "archived"


class ContentPiece(Base):
    __tablename__ = "aces_content_pieces"

    id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    brief_id: Mapped[uuid.UUID | None] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_content_briefs.id", ondelete="CASCADE"), nullable=True)
    brand_id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_brands.id", ondelete="CASCADE"), nullable=False)
    title: Mapped[str] = mapped_column(String(500), nullable=False)
    format: Mapped[str] = mapped_column(String(50), nullable=False, default=ContentFormat.REEL.value)
    platform: Mapped[str] = mapped_column(String(50), nullable=False, default=ContentPlatform.INSTAGRAM.value)
    raw_copy: Mapped[str | None] = mapped_column(Text, nullable=True)
    script_id: Mapped[uuid.UUID | None] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_scripts.id", ondelete="CASCADE"), nullable=True)
    status: Mapped[str] = mapped_column(String(50), nullable=False, default=PieceStatus.IDEA.value)
    publish_date: Mapped[str | None] = mapped_column(String(10), nullable=True)
    parent_piece_id: Mapped[uuid.UUID | None] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_content_pieces.id", ondelete="CASCADE"), nullable=True)
    account_id: Mapped[uuid.UUID | None] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("aces_social_accounts.id", ondelete="CASCADE"), nullable=True)
    tags: Mapped[list] = mapped_column(JSONB, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        Index("ix_aces_content_pieces_brand_id", "brand_id"),
        Index("ix_aces_content_pieces_status", "status"),
    )
