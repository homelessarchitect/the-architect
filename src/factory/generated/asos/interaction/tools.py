# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from datetime import datetime
from typing import Annotated, Any
from uuid import UUID


def _serialize_interaction(instance: Any) -> dict[str, Any]:
    from factory.generated.asos.interaction.serialize import serialize_interaction
    return serialize_interaction(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_interactions(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        lead_id: Annotated[str | None, "Filter by lead_id"] = None,
        agent_id: Annotated[str | None, "Filter by agent_id"] = None,
    ) -> list[dict[str, Any]]:
        """List interactions with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.interaction.repository import InteractionRepository
            repo = InteractionRepository(session)
            filters: dict[str, Any] = {}
            if lead_id is not None:
                from uuid import UUID as _UUID
                filters["lead_id"] = _UUID(lead_id)
            if agent_id is not None:
                filters["agent_id"] = agent_id
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_interaction(r) for r in results]

    @mcp.tool()
    async def get_interaction(
        id: Annotated[str, "The UUID of the interaction to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single interaction by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.interaction.repository import InteractionRepository
            repo = InteractionRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"Interaction with id '{id}' not found"}
        return _serialize_interaction(result)

    @mcp.tool()
    async def create_interaction(
        lead_id: Annotated[str, "UUID of lead"],
        agent_id: Annotated[str, "agent_id"],
        channel: Annotated[str, "email | instagram_dm | whatsapp | linkedin | sms | note"],
        timestamp: Annotated[datetime, "timestamp"],
        message: Annotated[str | None, "message"] = None,
        response: Annotated[str | None, "response"] = None,
    ) -> dict[str, Any]:
        """Create a new interaction."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.interaction.models import Interaction
            from factory.generated.asos.interaction.repository import InteractionRepository
            from uuid import UUID as _UUID
            entity = Interaction(
                lead_id=_UUID(lead_id) if lead_id is not None else None,
                agent_id=agent_id,
                channel=channel,
                message=message,
                response=response,
                timestamp=timestamp,
            )
            repo = InteractionRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_interaction(result)

    @mcp.tool()
    async def update_interaction(
        id: Annotated[str, "The UUID of the interaction to update"],
        agent_id: Annotated[str | None, "Updated agent_id"] = None,
        channel: Annotated[str | None, "Updated channel"] = None,
        message: Annotated[str | None, "Updated message"] = None,
        response: Annotated[str | None, "Updated response"] = None,
        timestamp: Annotated[datetime | None, "Updated timestamp"] = None,
    ) -> dict[str, Any]:
        """Update an existing interaction."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.interaction.repository import InteractionRepository
            from uuid import UUID as _UUID
            repo = InteractionRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"Interaction with id '{id}' not found"}
            if agent_id is not None:
                entity.agent_id = agent_id
            if channel is not None:
                entity.channel = channel
            if message is not None:
                entity.message = message
            if response is not None:
                entity.response = response
            if timestamp is not None:
                entity.timestamp = timestamp
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_interaction(entity)

    @mcp.tool()
    async def delete_interaction(
        id: Annotated[str, "The UUID of the interaction to delete"],
    ) -> dict[str, Any]:
        """Delete a interaction by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.interaction.repository import InteractionRepository
            from uuid import UUID as _UUID
            repo = InteractionRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"Interaction with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
