# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

import uuid
from datetime import datetime
from enum import StrEnum

from sqlalchemy import (
    DateTime,
    Integer,
    String,
    Text,
    ForeignKey,
    Index,
    func,
)
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from factory.core.database import Base



class OutreachTaskStatus(StrEnum):
    PENDING = "pending"
    SENT = "sent"
    RESPONDED = "responded"
    NO_RESPONSE = "no_response"
    SKIPPED = "skipped"


class OutreachTask(Base):
    __tablename__ = "asos_outreach_tasks"

    id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    campaign_id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("asos_campaigns.id", ondelete="CASCADE"), nullable=False)
    lead_id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("asos_leads.id", ondelete="CASCADE"), nullable=False)
    project_id: Mapped[uuid.UUID | None] = mapped_column(PG_UUID(as_uuid=True), ForeignKey("asos_projects.id", ondelete="CASCADE"), nullable=True)
    assigned_to: Mapped[str] = mapped_column(String(100), nullable=False, default='darien')
    channel: Mapped[str] = mapped_column(String(50), nullable=False)
    from_account: Mapped[str | None] = mapped_column(String(255), nullable=True)
    touch_number: Mapped[int] = mapped_column(Integer, nullable=False, default=1)
    due_date: Mapped[str | None] = mapped_column(String(10), nullable=True)
    message_draft: Mapped[str | None] = mapped_column(Text, nullable=True)
    status: Mapped[str] = mapped_column(String(50), nullable=False, default=OutreachTaskStatus.PENDING.value)
    response: Mapped[str | None] = mapped_column(Text, nullable=True)
    notes: Mapped[str | None] = mapped_column(Text, nullable=True)
    sent_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    responded_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        Index("ix_asos_outreach_tasks_campaign_id", "campaign_id"),
        Index("ix_asos_outreach_tasks_lead_id", "lead_id"),
        Index("ix_asos_outreach_tasks_status", "status"),
        Index("ix_asos_outreach_tasks_assigned_to", "assigned_to"),
        Index("ix_asos_outreach_tasks_due_date", "due_date"),
    )
