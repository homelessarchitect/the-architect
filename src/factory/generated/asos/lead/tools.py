# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_lead(instance: Any) -> dict[str, Any]:
    from factory.generated.asos.lead.serialize import serialize_lead
    return serialize_lead(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_leads(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        status: Annotated[str | None, "Filter by status"] = None,
        industry: Annotated[str | None, "Filter by industry"] = None,
        project_id: Annotated[str | None, "Filter by project_id"] = None,
    ) -> list[dict[str, Any]]:
        """List leads with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.lead.repository import LeadRepository
            repo = LeadRepository(session)
            filters: dict[str, Any] = {}
            if status is not None:
                filters["status"] = status
            if industry is not None:
                filters["industry"] = industry
            if project_id is not None:
                from uuid import UUID as _UUID
                filters["project_id"] = _UUID(project_id)
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_lead(r) for r in results]

    @mcp.tool()
    async def get_lead(
        id: Annotated[str, "The UUID of the lead to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single lead by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.lead.repository import LeadRepository
            repo = LeadRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"Lead with id '{id}' not found"}
        return _serialize_lead(result)

    @mcp.tool()
    async def create_lead(
        business_name: Annotated[str, "business_name"],
        project_id: Annotated[str | None, "UUID of project"] = None,
        name: Annotated[str | None, "name"] = None,
        industry: Annotated[str | None, "industry"] = None,
        website: Annotated[str | None, "website"] = None,
        instagram: Annotated[str | None, "instagram"] = None,
        email: Annotated[str | None, "email"] = None,
        phone: Annotated[str | None, "phone"] = None,
        lead_score: Annotated[int, "lead_score"] = 0,
        status: Annotated[str, "status"] = "new",
    ) -> dict[str, Any]:
        """Create a new lead."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.lead.models import Lead
            from factory.generated.asos.lead.repository import LeadRepository
            from uuid import UUID as _UUID
            entity = Lead(
                project_id=_UUID(project_id) if project_id is not None else None,
                name=name,
                business_name=business_name,
                industry=industry,
                website=website,
                instagram=instagram,
                email=email,
                phone=phone,
                lead_score=lead_score,
                status=status,
            )
            repo = LeadRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_lead(result)

    @mcp.tool()
    async def update_lead(
        id: Annotated[str, "The UUID of the lead to update"],
        name: Annotated[str | None, "Updated name"] = None,
        business_name: Annotated[str | None, "Updated business_name"] = None,
        industry: Annotated[str | None, "Updated industry"] = None,
        website: Annotated[str | None, "Updated website"] = None,
        instagram: Annotated[str | None, "Updated instagram"] = None,
        email: Annotated[str | None, "Updated email"] = None,
        phone: Annotated[str | None, "Updated phone"] = None,
        lead_score: Annotated[int | None, "Updated lead_score"] = None,
        status: Annotated[str | None, "Updated status"] = None,
    ) -> dict[str, Any]:
        """Update an existing lead."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.lead.repository import LeadRepository
            from uuid import UUID as _UUID
            repo = LeadRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"Lead with id '{id}' not found"}
            if name is not None:
                entity.name = name
            if business_name is not None:
                entity.business_name = business_name
            if industry is not None:
                entity.industry = industry
            if website is not None:
                entity.website = website
            if instagram is not None:
                entity.instagram = instagram
            if email is not None:
                entity.email = email
            if phone is not None:
                entity.phone = phone
            if lead_score is not None:
                entity.lead_score = lead_score
            if status is not None:
                entity.status = status
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_lead(entity)

    @mcp.tool()
    async def delete_lead(
        id: Annotated[str, "The UUID of the lead to delete"],
    ) -> dict[str, Any]:
        """Delete a lead by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.lead.repository import LeadRepository
            from uuid import UUID as _UUID
            repo = LeadRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"Lead with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
