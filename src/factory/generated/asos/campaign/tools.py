# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_campaign(instance: Any) -> dict[str, Any]:
    from factory.generated.asos.campaign.serialize import serialize_campaign
    return serialize_campaign(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_campaigns(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        project_id: Annotated[str | None, "Filter by project_id"] = None,
        status: Annotated[str | None, "Filter by status"] = None,
    ) -> list[dict[str, Any]]:
        """List campaigns with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.campaign.repository import CampaignRepository
            repo = CampaignRepository(session)
            filters: dict[str, Any] = {}
            if project_id is not None:
                from uuid import UUID as _UUID
                filters["project_id"] = _UUID(project_id)
            if status is not None:
                filters["status"] = status
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_campaign(r) for r in results]

    @mcp.tool()
    async def get_campaign(
        id: Annotated[str, "The UUID of the campaign to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single campaign by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.campaign.repository import CampaignRepository
            repo = CampaignRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"Campaign with id '{id}' not found"}
        return _serialize_campaign(result)

    @mcp.tool()
    async def create_campaign(
        project_id: Annotated[str, "UUID of project"],
        name: Annotated[str, "name"],
        description: Annotated[str | None, "description"] = None,
        goal: Annotated[str | None, "What success looks like: '5 demos booked in Colombia Q3 2026'"] = None,
        target_count: Annotated[int, "target_count"] = 0,
        status: Annotated[str, "status"] = "draft",
        start_date: Annotated[str | None, "ISO date YYYY-MM-DD (stored as str, date type unsupported in FieldDef)"] = None,
        end_date: Annotated[str | None, "ISO date YYYY-MM-DD (stored as str, date type unsupported in FieldDef)"] = None,
        channels: Annotated[list, "['instagram', 'whatsapp', 'email']"] = [],
        cadence_days: Annotated[list, "Touchpoint schedule in days from first contact: [0, 4, 11, 21, 35]"] = [],
        assigned_to: Annotated[str, "assigned_to"] = 'darien',
    ) -> dict[str, Any]:
        """Create a new campaign."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.campaign.models import Campaign
            from factory.generated.asos.campaign.repository import CampaignRepository
            from uuid import UUID as _UUID
            entity = Campaign(
                project_id=_UUID(project_id) if project_id is not None else None,
                name=name,
                description=description,
                goal=goal,
                target_count=target_count,
                status=status,
                start_date=start_date,
                end_date=end_date,
                channels=channels,
                cadence_days=cadence_days,
                assigned_to=assigned_to,
            )
            repo = CampaignRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_campaign(result)

    @mcp.tool()
    async def update_campaign(
        id: Annotated[str, "The UUID of the campaign to update"],
        name: Annotated[str | None, "Updated name"] = None,
        description: Annotated[str | None, "Updated description"] = None,
        goal: Annotated[str | None, "Updated goal"] = None,
        target_count: Annotated[int | None, "Updated target_count"] = None,
        status: Annotated[str | None, "Updated status"] = None,
        start_date: Annotated[str | None, "Updated start_date"] = None,
        end_date: Annotated[str | None, "Updated end_date"] = None,
        channels: Annotated[list | None, "Updated channels"] = None,
        cadence_days: Annotated[list | None, "Updated cadence_days"] = None,
        assigned_to: Annotated[str | None, "Updated assigned_to"] = None,
    ) -> dict[str, Any]:
        """Update an existing campaign."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.campaign.repository import CampaignRepository
            from uuid import UUID as _UUID
            repo = CampaignRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"Campaign with id '{id}' not found"}
            if name is not None:
                entity.name = name
            if description is not None:
                entity.description = description
            if goal is not None:
                entity.goal = goal
            if target_count is not None:
                entity.target_count = target_count
            if status is not None:
                entity.status = status
            if start_date is not None:
                entity.start_date = start_date
            if end_date is not None:
                entity.end_date = end_date
            if channels is not None:
                entity.channels = channels
            if cadence_days is not None:
                entity.cadence_days = cadence_days
            if assigned_to is not None:
                entity.assigned_to = assigned_to
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_campaign(entity)

    @mcp.tool()
    async def delete_campaign(
        id: Annotated[str, "The UUID of the campaign to delete"],
    ) -> dict[str, Any]:
        """Delete a campaign by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.campaign.repository import CampaignRepository
            from uuid import UUID as _UUID
            repo = CampaignRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"Campaign with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
