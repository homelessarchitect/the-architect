# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

from typing import Annotated, Any
from uuid import UUID


def _serialize_project(instance: Any) -> dict[str, Any]:
    from factory.generated.asos.project.serialize import serialize_project
    return serialize_project(instance)


def register(mcp: Any) -> None:

    @mcp.tool()
    async def list_projects(
        limit: Annotated[int, "Maximum number of results to return"] = 50,
        offset: Annotated[int, "Number of results to skip"] = 0,
        is_active: Annotated[bool | None, "Filter by is_active"] = None,
    ) -> list[dict[str, Any]]:
        """List projects with optional filters."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.project.repository import ProjectRepository
            repo = ProjectRepository(session)
            filters: dict[str, Any] = {}
            if is_active is not None:
                filters["is_active"] = is_active
            results = await repo.list(limit=limit, offset=offset, **filters)
        return [_serialize_project(r) for r in results]

    @mcp.tool()
    async def get_project(
        id: Annotated[str, "The UUID of the project to retrieve"],
    ) -> dict[str, Any] | None:
        """Get a single project by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.project.repository import ProjectRepository
            repo = ProjectRepository(session)
            from uuid import UUID as _UUID
            result = await repo.get(_UUID(id))
        if result is None:
            return {"error": f"Project with id '{id}' not found"}
        return _serialize_project(result)

    @mcp.tool()
    async def create_project(
        name: Annotated[str, "name"],
        description: Annotated[str | None, "description"] = None,
        icp: Annotated[dict, "Ideal Customer Profile — structured for agent reasoning"] = {},
        value_proposition: Annotated[str | None, "value_proposition"] = None,
        objections: Annotated[list, "[{trigger: str, response: str}]"] = [],
        scoring_rubric: Annotated[dict, "{high: [str], medium: [str], low: [str]}"] = {},
        channels: Annotated[list, "['email', 'instagram', 'whatsapp']"] = [],
        templates: Annotated[dict, "{channel_name: template_string}"] = {},
        is_active: Annotated[bool, "is_active"] = True,
    ) -> dict[str, Any]:
        """Create a new project."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.project.models import Project
            from factory.generated.asos.project.repository import ProjectRepository
            entity = Project(
                name=name,
                description=description,
                icp=icp,
                value_proposition=value_proposition,
                objections=objections,
                scoring_rubric=scoring_rubric,
                channels=channels,
                templates=templates,
                is_active=is_active,
            )
            repo = ProjectRepository(session)
            result = await repo.add(entity)
            await session.commit()
        return _serialize_project(result)

    @mcp.tool()
    async def update_project(
        id: Annotated[str, "The UUID of the project to update"],
        name: Annotated[str | None, "Updated name"] = None,
        description: Annotated[str | None, "Updated description"] = None,
        icp: Annotated[dict | None, "Updated icp"] = None,
        value_proposition: Annotated[str | None, "Updated value_proposition"] = None,
        objections: Annotated[list | None, "Updated objections"] = None,
        scoring_rubric: Annotated[dict | None, "Updated scoring_rubric"] = None,
        channels: Annotated[list | None, "Updated channels"] = None,
        templates: Annotated[dict | None, "Updated templates"] = None,
        is_active: Annotated[bool | None, "Updated is_active"] = None,
    ) -> dict[str, Any]:
        """Update an existing project."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.project.repository import ProjectRepository
            from uuid import UUID as _UUID
            repo = ProjectRepository(session)
            entity = await repo.get(_UUID(id))
            if entity is None:
                return {"error": f"Project with id '{id}' not found"}
            if name is not None:
                entity.name = name
            if description is not None:
                entity.description = description
            if icp is not None:
                entity.icp = icp
            if value_proposition is not None:
                entity.value_proposition = value_proposition
            if objections is not None:
                entity.objections = objections
            if scoring_rubric is not None:
                entity.scoring_rubric = scoring_rubric
            if channels is not None:
                entity.channels = channels
            if templates is not None:
                entity.templates = templates
            if is_active is not None:
                entity.is_active = is_active
            await session.flush()
            await session.refresh(entity)
            await session.commit()
        return _serialize_project(entity)

    @mcp.tool()
    async def delete_project(
        id: Annotated[str, "The UUID of the project to delete"],
    ) -> dict[str, Any]:
        """Delete a project by ID."""
        from factory.core.database import get_session_factory

        factory = get_session_factory()
        async with factory() as session:
            from factory.generated.asos.project.repository import ProjectRepository
            from uuid import UUID as _UUID
            repo = ProjectRepository(session)
            deleted = await repo.delete(_UUID(id))
            if not deleted:
                return {"error": f"Project with id '{id}' not found"}
            await session.commit()
        return {"deleted": True, "id": id}
