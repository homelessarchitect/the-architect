# AUTO-GENERATED by The Factory -- do not edit
from __future__ import annotations

import uuid
from datetime import datetime

from sqlalchemy import (
    DateTime,
    Boolean,
    String,
    Text,
    Index,
    func,
)
from sqlalchemy.dialects.postgresql import JSONB, UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from factory.core.database import Base



class Project(Base):
    __tablename__ = "asos_projects"

    id: Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    description: Mapped[str | None] = mapped_column(Text, nullable=True)
    icp: Mapped[dict] = mapped_column(JSONB, nullable=False)
    value_proposition: Mapped[str | None] = mapped_column(Text, nullable=True)
    objections: Mapped[list] = mapped_column(JSONB, nullable=False)
    scoring_rubric: Mapped[dict] = mapped_column(JSONB, nullable=False)
    channels: Mapped[list] = mapped_column(JSONB, nullable=False)
    templates: Mapped[dict] = mapped_column(JSONB, nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())

    __table_args__ = (
        Index("ix_asos_projects_is_active", "is_active"),
    )
